# =====================================================
# Docker Compose Override - Production with HTTPS
# =====================================================
# Usage: docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
# =====================================================

services:
  app:
    ports:
      - "443:8080"
    environment:
      - APP_URL=https://your-domain.com
      - APP_DEBUG=false
